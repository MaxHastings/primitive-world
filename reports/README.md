# Reports

Current release evidence: [RECURRENT_VALIDATION.md](RECURRENT_VALIDATION.md).
The authoritative runtime contract is [KERNEL_SPEC.md](../KERNEL_SPEC.md).

Older candidate, travel, sensing and GRU reports are historical evidence.
Their commands and schemas refer to Git tag `pre-recurrent-cutover`, not the
current executable. They remain intact; they are not current release claims.

Generated JSON/logs are ignored by default. The registered recurrent cutover
plan and compact summary are explicitly versioned with this release; raw local
runs are referenced by filename and SHA-256 in the validation record.
The released descendant bank lives in `policies/recurrent-v1.json`.
Do not overwrite prior experiments or relabel an unprepared initializer trained.
